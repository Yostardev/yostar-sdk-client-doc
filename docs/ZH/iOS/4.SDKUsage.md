**注意：为使用完整功能，以下代码只需复制、替换即可**
```objectivec
// 导入头文件
#import <YoSDKFBKit/YoSDKFBKit.h>
#import <YoSDKAiHelpKit/YoSDKAiHelpKit.h>
#import <YoSDKTWKit/YoSDKTWKit.h>

// 在以下方法中添加如下代码：
- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
    [YoFBAppDelegate yoFBApplication:application didFinishLaunchingWithOptions:launchOptions];
    [YoAiHelpAppDelegate yoAIHelpInitForApiKey:@"YOSTAR_app_59d69b706fdb43d6b51c43a907b61fbb" domainName:@"yostar@aihelp.net" appID:@"yostar_platform_4d06213b-51c0-4b03-881f-425fd1e3aa76"];
    return YES;
}

- (BOOL)application:(UIApplication *)app openURL:(NSURL *)url options:(NSDictionary<NSString *,id> *)options{
    BOOL FBHandled = [YoFBAppDelegate yoFBApplication:app openURL:url options:options];
    BOOL TWHandled = [YoTWAppDelegate yoTWApplication:app openURL:url options:options];
    return FBHandled||TWHandled;
    return  YES;
}

- (void)application:(UIApplication*)application didRegisterForRemoteNotificationsWithDeviceToken:(NSData*)deviceToken{
    [YoAiHelpAppDelegate yoAiHelpDidRegisterForRemoteNotificationsWithDeviceToken:deviceToken];
}

```
* 在使用类中导入头文件`#import <YoSDKCoreKit/YoSDKCoreKit.h>`

### 1. 创建单例
* 调用API

```objectivec
+ (instancetype)yoShareInstance;
```

* 调用实例

```objectivec
yoSDKManager = [YoSDKManager yoShareInstance];
```

### 2. 获取设备号
* 调用API

```objectivec
- (char *)yoGetDeviceID;
```

* 调用实例

```objectivec
char *deviceId = [yoSDKManager yoGetDeviceID];
```

### 3. 打开客服界面
* 调用API

```objectivec
//AIHelp
- (void)yoShowAiHelpFAQs:(const char *)sdkVersion
            withServerId:(const char *)serverId
             withRoleUid:(const char *)roleUid
            withRoleName:(const char *)roleName
      withRoleCreateTime:(const char *)roleCreateTime
            withPurchase:(int)purchase
                withTags:(const char *)tags;
```

+ 调用实例

```objectivec
 [yoSDKManager yoShowAiHelpFAQs:[@"1.0.3" UTF8String]
                   withServerId:[@"serverId" UTF8String]
                    withRoleUid:[@"roleId" UTF8String]
                   withRoleName:[@"roleName" UTF8String]
             withRoleCreateTime:[@"2020-09-16" UTF8String]
                   withPurchase:1000
                       withTags:[@"" UTF8String]];
```
* 接口参数说明

| 参数名称 | 参数类型 | 参数说明 |
| ------ | ------ | ------ |
| serverId | char | 服务器ID |
| roleUid | char | 角色ID |
| roleName  | char | 角色名称 |
| roleCreateTime  | char | 角色创建的时间（yyyy-MM-dd） |
| purchase  | int | 充值金额 |
| tags  | char | JSON格式化后的字符串数组 |

### 4. App Store 评分
* 调用API

```objectivec
+ (void)yoStoreReview;
```

* 调用实例

```objectivec
[YoStoreReview yoStoreReview];
```

### 5. 初始化
* 调用API

```objectivec
- (void)yoInitSDK:(const char *)baseUrl
       payStoreID:(const char *)payStoreId
            debug:(BOOL)isDebug;
```

* 调用实例

```objectivec
[yoSDKManager yoInitSDK:[@"http://www.url.com" UTF8String]
             payStoreID:[@"appstore" UTF8String]
                  debug:true];;
```

* 接口参数说明

|参数名称|参数类型|参数说明|
|----|----|----|
|baseUrl|char|网络请求地址|
|payStoreId|char|IPA传appstore|
|isDebug|bool|是否需要打印log|

### 6. 渠道登录
* 调用API

```objectivec
- (void)yoLogin:(int)platform
          param:(const char *)param
         param2:(const char *)param2
isCreateNewAccount:(BOOL)isCreateNewAccount;
```

* 调用实例

```objectivec
[yoSDKManager yoLogin:0
                param:[@"" UTF8String]
               param2:[@"" UTF8String]
   isCreateNewAccount:false];
```

* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|platform|NSInteger|登录的渠道有DEVICE(0),TRANSCODE(1),TWITTER(2),FACEBOOK(3),YOSTAR(4),APPLE(9)|是|
|param|NSString|当Platform的值为1时，param代表继承码。当Platform的值为4时，param为邮箱账号|否|
|param2|NSString|当Platform的值为1时，param2代表继承码对应的UID。当Platform的值为4时，param2为邮箱收到的验证码|否|
|isCreateNewAccount|BOOL|是否强制创建新的账号|是|


### 7. 快速登录
* 调用API

```objectivec
- (void)yoQuickLogin;
```

* 调用实例

```objectivec
[yoSDKManager yoQuickLogin];
```

### 8. 发行继承码
* 调用API

```objectivec
- (void)yoReqCode;
```

* 调用实例

```objectivec
[yoSDKManager yoReqCode];
```

### 9. 请求邮箱验证码
* 调用API

```objectivec
- (void)yoVerificationCodeReq:(const char *)email;
```

* 调用实例

```objectivec
[yoSDKManager yoVerificationCodeReq:[@"111@126.com" UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|email|char|要验证的邮箱账号|是|


### 10. 渠道绑定
* 调用API

```objectivec
- (void)yoBind:(int)platform
         email:(const char *)email
verificationCode:(const char *)code;
```

* 调用实例

```objectivec
[yoSDKManager yoBind:2
               email:[@"111@126.com" UTF8String]
    verificationCode:[@"456789" UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|platform|char|绑定使用的平台标识，渠道绑定参数标识可选择TWITTER(2),FACEBOOK(3),YOSTAR(4),APPLE(9)|是|
|email|char|当Platform的值为4时，email为邮箱账号|否|
|code|char|当Platform的值为4时，code为邮箱收到的验证码|否|


### 11. 解除绑定
* 调用API

```objectivec
- (void)yoUnBind:(int)platform
           email:(const char *)email
verificationCode:(const char *)code;
```

* 调用实例

```objectivec
[yoSDKManager yoUnBind:4
                 email:[@"111@126.com" UTF8String]
      verificationCode:[@"456789" UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|platform|char|绑定使用的平台标识，渠道绑定参数标识可选择TWITTER(2),FACEBOOK(3),YOSTAR(4),APPLE(9)|是|
|email|char|当Platform的值为4时，email为邮箱账号|否|
|code|char|当Platform的值为4时，code为邮箱收到的验证码|否|


### 12. 覆盖绑定
* 调用API

```objectivec
- (void)yoNewLink;
```

* 调用实例

```objectivec
[yoSDKManager yoNewLink];
```

### 13. 设置生日(可选，日本必接)
* 调用API

```objectivec
- (void)yoSetBirth:(const char *)birth;
```

* 调用实例

```objectivec
[yoSDKManager yoSetBirth:[@"456789" UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|birth|char|生日日期，日期格式为yyyyMMdd|是|

### 14. 统计事件上传
* 调用API

```objectivec
- (void)yoUserEventUpload:(const char *)strEventName
            withParameter:(const char *)strJsonCallbackParameter;
```

* 调用实例

```objectivec
NSDictionary *paramsDic = @{
        @"name" : @"ALIAS",
        @"age" : @"20"
    };
    NSData *data = [NSJSONSerialization dataWithJSONObject:paramsDic options:NSJSONWritingFragmentsAllowed error:nil];
    NSString *pStr = [[NSString alloc] initWithData:data encoding:NSUTF8StringEncoding];
[yoSDKManager yoUserEventUpload:[@"role_login" UTF8String]
                  withParameter:[pStr UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|strEventName|char|事件名称，要与AiriSDK后台添加的相对应|是|
|strJsonCallbackParameter|char|事件详情，为JSON格式的字符串参数|是|

### 15. 系统分享
* 调用API

```objectivec
- (void)yoSystemShare:(const char *)strShareText
        withImageData:(Byte *)imgDataByte
           withLength:(int)nSize;
```

* 调用实例

```objectivec
UIImage *image = info[UIImagePickerControllerOriginalImage];
NSData *imageData = UIImagePNGRepresentation(image);
[yoSDKManager yoSystemShare:[@"图片分享" UTF8String]
              withImageData:(Byte *)[imageData bytes]
                 withLength:(int)(imageData.length)];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|strShareText|char|要分享的文字内容，是否显示根据分享的平台支持情况定|是|
|shareImageData|Byte|要分享的data格式的图片|是|
|nSize|int|长度|是|


### 16. 支付
* 调用API

```objectivec
- (void)yoBuy:(const char *)productId
withServerTag:(const char *)serverTag
    withExtra:(const char *)extraData;
```

* 调用实例

```objectivec
[yoSDKManager yoBuy:[@"com.yostar.arknightsen.starter" UTF8String]
      withServerTag:[@"production" UTF8String]
          withExtra:[@"abc" UTF8String]];
```
* 接口参数说明

|参数名称|参数类型|参数说明|是否必须|
|----|----|----|----|
|productId|char|要购买的商品ID，要与支付渠道后台和AiriSDK后台配置的相符合|是|
|serverTag|char|服务器标识|是|
|extraData|char|附加参数，在支付结果回调时原样返回|是|

### 17. 删除或者恢复账户
* 调用API

```objectivec
- (void)yoDeletedOrNot:(YoUserActionType)userActionType;
```

* 调用实例

```objectivec
//删除
[yoSDKManager yoDeletedOrNot:YoUserActionTypeDelete];
//恢复
[yoSDKManager yoDeletedOrNot:YoUserActionTypeReborn];
```
**注意：调用该接口会清空用户缓存的账号数据**

|参数名称|参数类型|参数说明|
|----|----|----|
|userActionType|int|0 删除， 1 恢复|
